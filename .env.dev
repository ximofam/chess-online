# Server
SERVER_PORT=8080
SERVER_API_KEY=SERVER_API_KEY


# MySQL
DB_HOST=localhost
DB_PORT=3307
DB_NAME=chess_online
DB_ROOT_PASSWORD=123456

#JWT
JWT_SECRET_KEY=Author: Ximofam aka AVien Dep Trai 72
# Valid time units: s (seconds), m (minutes), h (hours).
# Note: 'd' (days) is NOT supported. Use hours instead (e.g., 24h).
# set access token time to live is 15 minutes
ACCESS_TOKEN_TTL=1000h 
# set refresh token time to live is 3 days
REFRESH_TOKEN_TTL=72h